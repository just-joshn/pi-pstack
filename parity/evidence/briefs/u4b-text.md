# U4b: shipped text and parity tooling on Pi mechanisms

Worktree: `/Users/josh-desktop/.pi/agent/pstack/pi-pstack-u4` (branch `u4-text-parity`, deps installed). Edit only `skills/`, `parity/`, and `package.json` scripts there. Commit on that branch in small commits. Do not push, no PR, do not touch the main clone `/Users/josh-desktop/.pi/agent/pstack/pi-pstack`, the other worktree `pi-pstack-u3`, or anything under `~/.pi/agent` outside your worktree. Another worker changes `extensions/` and `agents/` on its own branch.

Read first: `/tmp/pkg-briefs/a1-compliance-report.md` (all), `/tmp/pkg-briefs/u4-parity.md` (the earlier brief; its goals still hold, plus the changes below), `parity/PACKAGE-PLAN.md`, `parity/decisions-package.tsv`, `parity/build-from-live.mjs` (the one-shot migration and its REWRITES table), Pi docs `skills.md`, `packages.md`, `configuration.md`, `environment-variables.md` under `/Users/josh-desktop/.pi/agent/install/releases/0.87.1/node_modules/@earendil-works/pi-coding-agent/docs/`. Parity target: `parity/upstream/0.15.5/pstack/`. `parity/baseline/` holds a previous worker's baseline outputs of the live tools.

1. `build-from-live.mjs` is history now. Make it refuse to run unless `PSTACK_REBUILD_FROM_LIVE=1`, with one line saying the package is the source of truth.
2. Remove every `<pstack>` token from `skills/`. Per `skills.md` ("use relative paths from the skill directory"), a reference to a file in the same skill is skill-relative (`playbooks/x.md`, `scripts/watch-pr/watch-pr`), a reference to another pstack skill is `../<skill>/...` relative to the referring skill's directory, or the skill's name where upstream uses the name. Where upstream 0.15.5 has a sentence, move the Pi text back toward the upstream sentence. `pi-runtime.md` rows that named extension files by path name the tool or extension instead. Paths that must be absolute on disk (a plan file other agents read, the AGENTS.md block): the text says to resolve the relative path against the skill's directory and write the absolute result.
3. Agent-dir paths: where shipped text tells the model to read or write Pi's agent dir (`~/.pi/agent/AGENTS.md`, `~/.pi/agent/pstack/...`, `~/.pi/agent/extensions/pstack-agents.json`, sessions), say it once per file as "Pi's agent directory (`$PI_CODING_AGENT_DIR`, default `~/.pi/agent`)" and keep the default path readable. `setup-pstack` writes model scope to `extensions/pstack-agents.json` in that directory as `{ "modelScope": { "enforce": true, "allow": [...] } }`; keep that shape, since the runtime worker reads it.
4. Parity tooling (the earlier brief's steps 1 to 5): port `check-parity.mjs`, `full-audit.mjs`, `sync-check.mjs` to read the repo root, upstream `parity/upstream/current`; `parity/probes` tests import repo files relatively; write `parity/lint-package.mjs` with mutation tests. The lint also fails on any `<pstack>` token and any `homedir()` joined with `.pi/agent` in `extensions/`. It must report, not fix, violations in `extensions/` and `agents/` (the other worker owns those; they will be clean after the merge). Any allowlist or `pi-additions.tsv` row you add carries its reason; no generic weakening. `full-audit` coverage stays at or above its floor.
5. `npm run check` must reference only scripts that exist.

Checks: all parity tools, the lint, and `bun test parity/probes` from the worktree root; paste final lines next to `parity/baseline/` lines. List every allowlist or justification row added.

Reply with: commits (sha, subject), baseline vs new outputs, added rows with reasons, lint mutation results, the lint's findings in `extensions/` and `agents/`, anything left undone.
