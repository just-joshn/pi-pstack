# A1: Pi docs compliance and Cursor-to-Pi mechanism audit (read-only)

You are read-only. Write exactly one file, your report, at `/tmp/pkg-briefs/a1-compliance-report.md` (use bash `cat > file` for that single write). Change nothing else.

## Inputs

- Package under audit: `/Users/josh-desktop/.pi/agent/pstack/pi-pstack` (a Pi package; manifest in `package.json` under `pi`). Plan: `parity/PACKAGE-PLAN.md`. Decisions so far: `parity/decisions-package.tsv`. How it was built: `parity/build-from-live.mjs`.
- Official Pi docs, installed version 0.87.1 (authoritative): `/Users/josh-desktop/.pi/agent/install/releases/0.87.1/node_modules/@earendil-works/pi-coding-agent/docs/`. Read every file that bears on a package that ships extensions, skills, and agent definitions: at least `packages.md extensions.md skills.md settings.md configuration.md prompt-templates.md session-format.md sessions.md tui.md rpc-extension-ui.md security.md slash-commands.md message-types.md compaction.md environment-variables.md cli.md how-pi-works.md sdk.md json.md`. Skim the rest and include any rule that applies.
- Exact types: `../dist/core/extensions/types.d.ts` next to the docs.
- Official examples: `../examples/extensions/` (especially `subagent/`, `sandbox/`, `preset.ts`, `todo.ts`, `questionnaire.ts`, `plan-mode/`, `with-deps/`, `dynamic-resources/`, `protected-paths.ts`, `permission-gate.ts`, `truncated-tool.ts`, `dynamic-tools.ts`).
- Official upstream Cursor pstack 0.15.5 (the parity target): `parity/upstream/0.15.5/pstack/` in the package, including `.cursor-plugin/plugin.json`. Cursor CLI tool surface notes: `/tmp/r3-own/cursor-cli-surface.md`.

## Deliverable sections

1. **Rule matrix.** Every normative statement in the docs and types that applies to this package ("do", "do not", "use", "prefer", "must", "should", documented defaults a package relies on). One row per rule: rule, citation (`doc.md#section` or `types.d.ts:line`), verdict for the package (COMPLIES, VIOLATES, N/A), evidence (`file:line`). Check the code, not the comments. Cover at minimum: package manifest and `files`, dependency placement (peer `*`, no bundling), resource discovery, extension factory lifecycle (no processes, watchers, timers, sockets in the factory; `session_start` / idempotent `session_shutdown`), reload safety, tool definitions (TypeBox schema, `content` plus `details`, throwing for errors, truncation of large output, `withFileMutationQueue` for file-mutating tools, sequential execution for shared state, nested-call `usage`), `ctx.mode` and `ctx.hasUI` guards for print, JSON and RPC modes, state storage choice (tool-result details vs `appendEntry` vs `sendMessage` vs external), branch-correct reconstruction from `getBranch()`, renderers for custom entries, command-only APIs not called from event handlers, `before_agent_start` usage (prompt sections vs replacing the system prompt), `tool_call` blocking semantics, skill frontmatter and naming rules, relative paths in skills, `disable-model-invocation`, security and trust notes, config file locations.
2. **Mechanism map.** For every mechanism the Cursor plugin relies on (plugin manifest, skills, agents, `mode: true` / `reminder:`, `is_background`, Task tool and its arguments, Await, Shell with notifications, `/loop`, `/goal`, AskQuestion, TodoWrite, cloud agents, user rules, `~/.cursor/...` paths, plugin-relative paths such as `pstack/skills/...`), give: the Pi mechanism the package uses now (`file:line`), whether that mechanism is documented Pi (cite) or invented by this port, and for each invention the documented Pi mechanism that should replace it, or "no documented Pi mechanism exists" with the search you did. Known suspects to settle explicitly: the `<pstack>` path token in shipped text and agent bodies; the bundled `agents/` directory and agent discovery; `~/.pi/agent/extensions/pstack-agents.json` as config; the pstack block and reminder line that `setup-pstack` writes into `~/.pi/agent/AGENTS.md`; the `pstack-mode` extension's per-turn reminder and named-skill injection; spawning child `pi` processes for Task (compare `examples/extensions/subagent/`); the detached runner that is meant to outlive the parent; `Shell`/`Await`; goals; hidden principle skills; the skill write guard and bash guards in `pstack-guards.ts`; `questionnaire` and `todo` versus the official examples of the same names.
3. **Parity risks from switching to documented mechanisms.** For each replacement in section 2, say whether it changes behavior against Cursor 0.15.5 and how to keep parity.
4. **Ranked fix list.** VIOLATES rows and inventions, ranked by severity (breaks in some mode or install type, then doc violation, then style). Each with the smallest concrete fix.

Be exhaustive and literal; cite line numbers. Do not guess; mark anything you could not confirm as UNCONFIRMED with what you tried.
