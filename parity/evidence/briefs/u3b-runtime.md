# U3b: runtime fixes from the Pi compliance audit

Worktree: `/Users/josh-desktop/.pi/agent/pstack/pi-pstack-u3` (branch `u3-runtime`, deps installed). Edit only `extensions/` and `agents/` there. Commit your work on that branch in small commits (one per item below). Do not push, do not open a PR, do not touch the main clone `/Users/josh-desktop/.pi/agent/pstack/pi-pstack` or anything under `~/.pi/agent` outside your worktree. Another worker is fixing quit survival in `runs.ts`/`runner.mjs` lifecycle code in the main clone; keep your changes to those two files minimal so the merge stays easy.

Read first: the audit `/tmp/pkg-briefs/a1-compliance-report.md` (all of it), `parity/PACKAGE-PLAN.md`, Pi docs `/Users/josh-desktop/.pi/agent/install/releases/0.87.1/node_modules/@earendil-works/pi-coding-agent/docs/extensions.md` and `packages.md`, types `../dist/core/extensions/types.d.ts`, and the official examples `../examples/extensions/{subagent,sandbox,truncated-tool.ts,todo.ts}`. Parity target is Cursor pstack 0.15.5 at `parity/upstream/0.15.5/pstack/`. Cursor CLI bundle (for the real Task contract): `~/.local/share/cursor-agent/versions/2026.09.18-9a7762b/index.js` (grep `TaskToolCallArgsProto`, `ShellArgs`).

Rule for every item: documented Pi mechanism, Cursor 0.15.5 behavior. A behavior test that fails before and passes after (`bun test extensions`, bun:test, assert literal values).

1. Bundled agents. Agent discovery loads the package's `agents/*.md` (resolved from `import.meta.url`) at the lowest precedence; user `getAgentDir()/agents` and trusted project `.pi/agents` override by name. Test with an empty agent dir: `generalPurpose`, `poteto-agent`, `pstack-general`, `pstack-reader`, `Comment Sicko` resolve.
2. Agent-level `is_background` (Cursor frontmatter, upstream `agents/poteto-agent.md:4`). Parse it; when a Task call omits `run_in_background`, the agent's `is_background` decides; an explicit argument wins. Restore upstream `is_background: true` in `agents/poteto-agent.md`.
3. No `<pstack>` token. `agents/poteto-agent.md` body: restore the upstream sentence (read the `poteto-mode` skill's `SKILL.md` ... navigate to a leaf `principle-*` skill) and make the path resolvable the Pi way: the extension appends to the agent's system prompt one line giving the absolute paths of the skills the body names, computed from the package root, or passes `--skill <path>` for poteto-mode to the child. Choose whichever the Pi docs support better (`cli.md` `--skill`, `skills.md`), explain the choice, test it.
4. Guards in every child from the package: `extensions/pstack-guards.ts` resolved from `import.meta.url`, not the agent dir.
5. `extensions/pstack-guards.ts` and `extensions/pstack-mode.ts`: no `homedir()/.pi/agent`; use `getAgentDir()` for agent-dir paths and package-relative paths (from `import.meta.url`) for package files. The skill write guard protects `getAgentDir()/skills` and the package `skills/`. `pstack-mode.ts` reminder and named-skill text use the resolved absolute `poteto-mode` paths; wording otherwise unchanged.
6. Model scope: read `getAgentDir()/extensions/pstack-agents.json` `{ "modelScope": {...} }` (the official `sandbox.json` precedent). No fallback to `settings.json`.
7. Failures throw. Every Task/Shell/Await/SubagentAwait/goal execution failure throws from `execute()` (Pi marks thrown errors as failed tool results); remove returned `isError`. Expected non-failure outcomes (timeout with state, detached wait) stay normal content.
8. Bounded output. Task and Shell model-facing text uses Pi's truncation helpers (see `examples/extensions/truncated-tool.ts`) with the full transcript or log path in the text.
9. Nested usage. Sum the child's assistant-message `usage` into the Task tool result `usage` so session totals include subagents.
10. `todo.ts`: `executionMode: "sequential"`, with a concurrent-call test.
11. `Task.machine`: check the Cursor CLI bundle's Task args. If Cursor has it and it has no Pi equivalent, keep the argument and throw a clear "not supported on Pi" error from `execute()`; if Cursor lacks it, remove it from the schema.
12. Fix the 7 baseline typecheck errors (`npm run typecheck`): no `any`, no `@ts-ignore`, no casts hiding real mismatches.

Checks: `bun test extensions` (baseline 69), `npm run typecheck` 0 errors, and a real run: isolated agent dir (`S=$(mktemp -d)`, `mkdir -p $S/agent $S/cwd`, copy `~/.pi/agent/auth.json`, `jq 'del(.subagents,.packages)' ~/.pi/agent/settings.json > $S/agent/settings.json`, `PI_CODING_AGENT_DIR=$S/agent pi install <your worktree>`, `git init $S/cwd`), all `PI_*` unset except `PI_CODING_AGENT_DIR`, `pi --print --no-session` from `$S/cwd` asking for one Task with `subagent_type: "pstack-general"`, `model: "openai-codex/gpt-6-luna:low"`, whose child reads `<worktree>/skills/poteto-mode/playbooks/investigation.md`. Expect: agent resolves with no agents copied into `$S/agent`, and the child's read is blocked by the guard. Paste output.

Reply with: commits (sha, subject), item-to-test map, check outputs, the item 3 choice with its doc citation, anything left undone.

## Settled design (read before anything else)

The design is settled. The coordinator already ran `how`, `why`, and the architect arena for this brief: synthesized design `/tmp/arena-u3b-runtime-70fc1e6-01a0cbc6/synthesized-design.md`, rationale `rationale.md`, cross-judge `cross-judge.md` in the same dir. Implement that design. In your Feature todo list, mark the `how` and `architect` steps `skip: done by coordinator, see /tmp/arena-u3b-runtime-70fc1e6-01a0cbc6/`. Write the code yourself in this session, item by item, committing each; do not spawn Tasks for implementation, investigation, or design. One optional Task at the end is allowed for an independent review of the full diff.
