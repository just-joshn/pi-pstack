# U3c: runtime follow-ups found in audit

Worktree: `/Users/josh-desktop/.pi/agent/pstack/pi-pstack-u3` (branch `u3-runtime`, clean at `1d5ca7c`). Edit only `extensions/` and `agents/`. One commit per item. No push, no PR, nothing outside this worktree.

The design is settled by the coordinator. Do not run the Feature playbook's `how` or `architect` steps (mark them `skip: settled by coordinator`). Write the code yourself; do not spawn Tasks. Each item gets a bun:test behavior test that fails before and passes after. Pi docs: `/Users/josh-desktop/.pi/agent/install/releases/0.87.1/node_modules/@earendil-works/pi-coding-agent/docs/`.

1. F-A. `poteto-agent` children must be handed poteto-mode deterministically. Measured: the agent inherits skills, so `--no-skills` is never passed; poteto-mode has `disable-model-invocation: true`, so it is absent from the child's skill list and `--skill` adds nothing visible; a probe child found the path only by inferring it from sibling skill locations. Pi's documented way to force a skill is `/skill:name` (`skills.md`). Verified headless: `pi --mode json '/skill:poteto-mode <text>'` expands to `<skill name="poteto-mode" location="<abs path>">...` in the user message. Make the child's prompt start with `/skill:poteto-mode ` for agents that declare it, via a Pi-only agent frontmatter field (for example `skills: poteto-mode`) instead of the hardcoded `agent.name === "poteto-agent"` check; drop the `--skill` flag if it becomes redundant. Real proof: run a `poteto-agent` Task from an isolated agent dir with the package installed and show the child transcript's first user message contains the `<skill name="poteto-mode" location=...>` block.
2. F-E. Nested usage must count once. Today `store.usage(id, attempt)` is attached to every result that sees a terminal run (foreground Task, each Await/SubagentAwait, Task interrupt on a finished run), so awaiting a finished run twice double-counts session cost. Attach usage exactly once per run attempt (first result that observes the terminal state; persist the claim so it survives restart, like the existing acks). Test: await a finished run twice and interrupt it; exactly one result carries `usage`.
3. F-G. `agents/comment-sicko.md:31` hard-codes `~/.pi/agent/skills/how/SKILL.md` and `why`. Match the upstream sentence in `parity/upstream/0.15.5/pstack/agents/comment-sicko.md` (skill names). The package lint (`parity/lint-package.mjs` on the other branch) flags it.
4. F-H. Cursor's Task args (bundle `~/.local/share/cursor-agent/versions/2026.09.18-9a7762b/index.js`, `TaskToolCallArgsProto`) include `cloud_requested_environment_build_id` (field 11). Accept it and throw "not supported on Pi" when set, like `machine`.

Checks: `bun test extensions` (baseline 81), `npm run typecheck` (0). Reply with commits, tests, check outputs, and the F-A real-proof lines.
