# K: runtime and skill fixes from re-audit A2 (settled design)

Worktree: `/Users/josh-desktop/.pi/agent/pstack/pi-pstack-k` (branch `k-fixes`, deps installed). Edit only `extensions/`, `skills/make-bot-ui/`, `skills/setup-pstack/`, and the matching `parity/pi-additions.tsv` rows. One commit per item. No push, no PR, nothing outside this worktree, never kill processes you did not start.

The design is settled by the coordinator. Mark the Feature playbook's `how` and `architect` steps `skip: settled by coordinator, see /tmp/pkg-briefs/k-fixes.md`. Write the code yourself; do not spawn Tasks. Each code item gets a bun:test behavior test that fails before and passes after. Evidence: `parity/evidence/a2/a2a-codex.md` (findings 1, 2, 4, 5). Cursor CLI bundle for contracts: `~/.local/share/cursor-agent/versions/2026.09.18-9a7762b/index.js`. Pi docs: `/Users/josh-desktop/.pi/agent/install/releases/0.87.1/node_modules/@earendil-works/pi-coding-agent/docs/`.

1. Model scope fails closed (`extensions/pstack-agents/agents.ts` `loadModelScope`/`parseModelScope`). Missing file (ENOENT) means no policy. Any other read error, invalid JSON, or a present `modelScope` of the wrong shape throws a clear error naming the file, so Task refuses to launch. Tests: unreadable file, wrong-shaped key, valid file under a non-default agent dir. In `skills/setup-pstack/SKILL.md`, the write targets are `${PI_CODING_AGENT_DIR:-$HOME/.pi/agent}/AGENTS.md` and `${PI_CODING_AGENT_DIR:-$HOME/.pi/agent}/extensions/pstack-agents.json` wherever the text tells the model where to write (prose that only names the location may keep `~/.pi/agent`).
2. `SubagentAwait` returns the final message. Cursor's `SubagentAwaitComplete` carries `agent_id`, `transcript_path`, `tool_call_count`, `final_message`. When the run is terminal, include a bounded excerpt of `store.finalOutput(id)` (same truncation helper and full-transcript pointer as the foreground Task result) plus the transcript path. Test the terminal result text.
3. Shell `output_notification` matches Cursor's `ShellOutputNotificationConfig` (`pattern`, `reason`, optional `debounce` seconds, optional `notification_limit`). Accept either the existing regex string (Pi shorthand, same as `{pattern}`) or that object. Reject a pattern over 500 characters. Default `notification_limit` is 100, as in Cursor (`this.limit=e.config.notificationLimit??100`). After the limit, send one final notice that says no further notifications will be sent for this pattern and names the output log (Cursor's wording: search the bundle for "notifications will be sent for this pattern"). Honor `debounce` by coalescing matches within the window into one notice. Include `reason` in the notice text when given. Tests: limit reached with the final notice, debounce coalescing, object and string forms, the 500-character cap. Update `skills/poteto-mode/references/pi-runtime.md`'s Shell row to document both forms (edit only that row there).
4. `skills/make-bot-ui/SKILL.md` "Hand the bot a secret". Today the secret sits in a file under the bot's directory and is passed to the bot's Pi process environment, so the bot's `read` or shell tools can reach it. Cursor's secret-request connector never exposes the value to the model. The Pi form: the user stores the secret in the OS credential store (macOS `security add-generic-password -a <slug> -s pstack-bot -w`, Linux `secret-tool store`), the local page server reads it and performs the credentialed request itself on the bot's behalf through a narrow action the routine names, and the bot run gets no secret in its environment and runs without shell tools (`--tools` excludes `bash` and `Shell`), so nothing in its reach can read the store. Rewrite the section and any step that passes the secret into the bot run; keep upstream sentences where they still hold; update the matching `parity/pi-additions.tsv` rows with the reason "Pi has no secret connector; the local server holds the credential and the bot cannot reach it". Run `node parity/check-parity.mjs` and `node parity/full-audit.mjs` until both are clean.

Checks: `env -u PSTACK_AGENTS_DEPTH -u PSTACK_AGENTS_AGENT -u PSTACK_AGENTS_RUN_ID -u PSTACK_AGENTS_NESTING_ALLOWED bun test extensions parity/probes` (baseline 126), `npm run typecheck` 0, `node parity/lint-package.mjs` 0, parity tools clean. Reply with commits, tests, and check outputs.
