# pstack-pi compatibility report

Generated by `node compat/report.mjs` (`npm run compat:report`). Do not edit by hand. `npm run compat:gate` fails when this file is stale.

## 1. Pinned upstream revision

- Repository: https://github.com/cursor/plugins
- Path in repository: pstack
- Commit: 5bf2b1544db739998121a306340631963c2ff3de
- Upstream plugin version: 0.15.2
- Captured at: 2026-09-15T00:00:00Z
- Manifest digest: git-tree-sha1 f235052692fba08f7ff40df62775339cb4794c4c
- Scoped directories: skills, agents, automations, docs
- Normative pin: `upstream.lock.json` is the compatibility-cycle authority. `port/upstream.json` is the machine-consumed copy and must carry the same commit.
- Note: Content-verified equivalent to c1c0a32802223f4be824112dd83d33ad29a8b26c: zero commits touch pstack/ between 5bf2b15 and c1c0a32, and the pstack/ git tree SHA is identical at both revisions (f235052...). port/upstream.json now pins the same SHA as this lock file so the two never diverge.
- Note: This file is the normative pin for the compatibility cycle per the pstack-pi refactor mandate. port/upstream.json is the machine-consumed copy read by port/port.mjs; keep upstream.lock.json.commit.sha and port/upstream.json.commit equal. port/drift-report.mjs enforces that invariant and reports upstream drift past this pin.

## 2. Architecture before and after

Before the refactor the tree held the byte-pinned upstream content (`port/`, `skills/`, `agents/`, `automations/`, `docs/`) over a behavioral ledger (`spec/`), with runtime extensions reproducing Cursor host semantics, a layered test runner, and docs. The mandate's machine-readable matrix, task policy, integration registry, loop FSM, hosted worker, and benny service did not exist (`.pi/refactor/RECON.md` section 1).

The current tree adds `compat/` (the generated parity matrix, capabilities, authored dependency dispositions, and this report), `extensions/agents/` policy, `extensions/integrations/`, `extensions/loop/`, `services/worker/` with `extensions/hosted/`, and `services/benny/`. The ledger in `spec/contracts/*.tsv` stays the single source of truth, and `npm run compat:gate` proves the closed program.

## 3. Extensions and their responsibilities

One row per directory under `extensions/`.

| Directory | Responsibility |
| --- | --- |
| `extensions/agents` | pstack_task policy-complete entrypoint, the eight-axis policy compiler, and the child policy guard |
| `extensions/benny` | pstack_benny_wake, /setup-benny, /benny-triage, and /benny-repro over the wake file |
| `extensions/commands` | generated /skill:<name> and /<name> command shims |
| `extensions/companions` | pstack_deslop, pstack_control_cli, and pstack_control_ui |
| `extensions/decision-log` | pstack_decision_log TSV rows under the cwd .pi allowlist |
| `extensions/gates` | /pstack-gates fail-closed pre-ship check |
| `extensions/heartbeat` | the pstack_loop timer and its settle/watch coalescing |
| `extensions/hosted` | the services/worker client for environment=hosted runs |
| `extensions/integrations` | the integration capability registry and pstack_integrations coverage reporting |
| `extensions/loop` | the durable /loop run record and the DEFINE_PREDICATE..COMPLETE/BLOCKED reducer |
| `extensions/models` | /setup-pstack config, budget labels, and role model resolution |
| `extensions/orchestration` | pstack_swarm and pstack_arena parallel fan-out |
| `extensions/poteto-state` | sticky poteto-mode state and the todo store |
| `extensions/readonly-state` | session readonly policy and tool coercion |
| `extensions/sessions` | pstack_sessions list, grep, current, and recall ranking |
| `extensions/shipping` | pstack_babysit, pstack_ship, and the shared merge gate evaluator |
| `extensions/subagents` | pstack_spawn, pstack_jobs, and the child runner |
| `extensions/test` | test-only peer-dependency shims and extension harness entrypoints |
| `extensions/worktree` | pstack_worktree create, list, remove, prune, and shutdown cleanup |

The composition files `extensions/index.ts`, `extensions/effects.ts`, and the `sticky-*.ts` modules sit at the root rather than in a directory.

## 4. Upstream inventory totals

Inventory rows are the `inv-*` rows in `compat/parity.json`, one per in-scope artifact of the pinned tree plus the root artifacts.

- Inventory rows: 152
- Scoped artifacts (skills/, agents/, automations/, docs/): 147
- Root artifacts: 5 (README.md, LICENSE, .gitignore, assets/logo.png, .cursor-plugin/plugin.json)
- By status: unimplemented=0, implemented=0, verified=151, blocked=1

## 5. Parity totals by classification

Totals over every row in `compat/parity.json` (ledger rows plus inventory rows).

| Classification | Rows |
| --- | --- |
| `EXACT-CONTRACT` | 217 |
| `ADAPTED-EQUIVALENT` | 220 |
| `HOSTED-CAPABILITY-REQUIRED` | 6 |
| `APPROVED-EXCEPTION` | 3 |

## 6. Parity totals by verification status

- unimplemented=6, implemented=0, verified=437, blocked=3

Capability rollup (`compat/capabilities.json`): 13 capabilities, 13 verified, 2 hosted-required.

## 7. Hosted prerequisites

Rows classified `HOSTED-CAPABILITY-REQUIRED` need a service that does not run locally.

| Row | Upstream path | Prerequisites |
| --- | --- | --- |
| `ceiling-02` | `cloud-agents` | services/worker; twin surface worktree is the local fallback until services/worker ships |
| `ceiling-03` | `automations-slack` | services/benny; twin surface benny is the local fallback until services/benny ships |
| `ceiling-04` | `grok-bot` | services/benny; twin surface benny is the local fallback until services/benny ships |
| `ceiling-05` | `mcp` | services/worker; twin surface spawn is the local fallback until services/worker ships |
| `ceiling-08` | `durable-jobs` | services/worker; twin surface jobs is the local fallback until services/worker ships |
| `ceiling-09` | `ide-driving` | services/worker; twin surface control is the local fallback until services/worker ships |

**Every prerequisite named in rows**

- services/benny
- services/worker
- twin surface benny is the local fallback until services/benny ships
- twin surface control is the local fallback until services/worker ships
- twin surface jobs is the local fallback until services/worker ships
- twin surface spawn is the local fallback until services/worker ships
- twin surface worktree is the local fallback until services/worker ships

**Authored hosted dispositions (`compat/dependencies.json`)**

| Mechanism | piMechanism | Verification |
| --- | --- | --- |
| `cloud-agents` | hosted: services/worker (worker protocol) | `tests/hosted/worker.test.mjs` |
| `automations-slack` | hosted: services/benny (receiver) | `tests/hosted/benny-events.test.mjs` |
| `grok-bot` | hosted: services/benny (receiver) | `tests/hosted/benny-events.test.mjs` |
| `mcp` | hosted: services/worker (MCP-capable host) | `tests/hosted/worker.test.mjs` |
| `durable-jobs` | hosted: services/worker (durable run store) | `tests/hosted/failure-modes.test.mjs` |
| `ide-driving` | hosted: services/worker (IDE-driving host) | `tests/hosted/worker.test.mjs` |

## 8. Approved exceptions

Every `APPROVED-EXCEPTION` row carries a non-null `exceptionJustification`.

| Row | Upstream path | Justification |
| --- | --- | --- |
| `ceiling-01` | `marketplace` | Cursor marketplace installs need Cursor's own registry and .cursor-plugin loader, and no Pi API registers a marketplace, so the package installs from the Pi manifest and the content surface stays the twin. |
| `ceiling-06` | `sticky-host` | Cursor renders reminder, mode, icon, and color from skill frontmatter; Pi's frontmatter schema is closed and exposes no hook to register host chrome, so those four fields drop while the sticky surface reproduces the mode. |
| `inv-cursor-plugin-plugin-json` | `.cursor-plugin/plugin.json` | Cursor's .cursor-plugin loader has no Pi equivalent; the Pi package manifest replaces it and the content surface is the twin. |

## 9. Differential and conformance test results

Committed summary from `spec/differential-results.json`. This report never executes tests and reports only the stable fields; the runner stamps its own `generatedAt`.

- Upstream commit: 5bf2b1544db739998121a306340631963c2ff3de
- Cases: 12, equal: 12, differ: 0

Named scripts: `npm run test:differential` executes the differential fixtures, `npm run conformance` runs the AGENTS.md conformance layer, and `npm test` runs the full layered suite.

## 10. Unresolved parity defects

Rows with status `blocked` or `unimplemented` in `compat/parity.json`. These are declared non-parity states, not silent gaps.

| Row | Upstream path | Classification | Status |
| --- | --- | --- | --- |
| `ceiling-01` | `marketplace` | APPROVED-EXCEPTION | blocked |
| `ceiling-02` | `cloud-agents` | HOSTED-CAPABILITY-REQUIRED | unimplemented |
| `ceiling-03` | `automations-slack` | HOSTED-CAPABILITY-REQUIRED | unimplemented |
| `ceiling-04` | `grok-bot` | HOSTED-CAPABILITY-REQUIRED | unimplemented |
| `ceiling-05` | `mcp` | HOSTED-CAPABILITY-REQUIRED | unimplemented |
| `ceiling-06` | `sticky-host` | APPROVED-EXCEPTION | blocked |
| `ceiling-08` | `durable-jobs` | HOSTED-CAPABILITY-REQUIRED | unimplemented |
| `ceiling-09` | `ide-driving` | HOSTED-CAPABILITY-REQUIRED | unimplemented |
| `inv-cursor-plugin-plugin-json` | `.cursor-plugin/plugin.json` | APPROVED-EXCEPTION | blocked |

Grouped by reason: 6 unimplemented hosted ceiling row(s) wait on a hosted service, and 3 blocked approved-exception row(s) stand in for absent host chrome or a Cursor-only loader.

## 11. Migration instructions

Install the Pi package from a checkout.

```bash
pi install /absolute/path/to/pi-pstack
```

Regenerate the derived artifacts after any ledger or root-artifact change.

```bash
npm run compat:generate   # compat/parity.json and compat/capabilities.json
npm run compat:report     # compat/REPORT.md
```

`npm run compat:check` fails when either generated JSON is stale, and `node compat/report.mjs --check` fails when this report is stale.

## 12. Exact commands

```bash
pi install /absolute/path/to/pi-pstack
npm run compat:gate
npm run compat:check
npm run compat:report
npm run spec:check
npm run test:differential
npm run test:hosted
npm run conformance
npm run parity:check
```
